# Copy to `.dev.vars` for local `wrangler dev`. NEVER commit the real file.
# In production these two are set as encrypted secrets via:
#   wrangler secret put GMAIL_USER
#   wrangler secret put GMAIL_APP_PASSWORD
GMAIL_USER="your-clinic-gmail@gmail.com"
GMAIL_APP_PASSWORD="abcdefghijklmnop"
